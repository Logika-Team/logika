# Camp Hero Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task.

**Goal:** Add the approved Figma camp hero to `source/camp.html` by reusing the existing Logika hero structure and replacing only camp-specific content, imagery, and scoped styling.

**Architecture:** Keep the shared header/footer, `main-form`, button classes, fonts, icons, and JavaScript entrypoints. Add a `camp-hero` modifier to the existing `banner-section`; do not alter homepage/about hero behavior. Copy only the four raster images referenced by Figma node `449:106441` into a semantic camp asset directory.

**Tech Stack:** Static HTML partials, SCSS, Gulp 4, existing Montserrat assets, existing browser-sync/static build workflow.

## Global Constraints

- Work directly in `/home/sbaikov/Desktop/Projects/logika` on branch `main`; do not create a worktree.
- Preserve existing source partials and shared hero behavior.
- Use the four assets referenced by Figma node `449:106441`; do not copy the entire `.fig` export.
- Keep camp-only CSS under `.camp-hero` and use existing CSS variables/breakpoints.
- Do not add dependencies or change backend/WordPress code.
- Keep the form compatible with the existing `main.js` validation path.

## Files and responsibilities

- Create: `source/camp.html` — camp page shell and the hero/information bar markup.
- Create: `source/img/camp/hands.png` — Figma asset `816151e2...`.
- Create: `source/img/camp/pool.png` — Figma asset `bcb3fbcb...`.
- Create: `source/img/camp/team.png` — Figma asset `0704fdf7...`.
- Create: `source/img/camp/mountains.png` — Figma asset `4a107976...`.
- Modify: `source/scss/blocks/sections/banner-section.scss` — only `.camp-hero` modifier styles and collage rules.
- Generate: `build/camp.html`, `build/css/blocks/sections/banner-section.css`, and copied build assets through the existing Gulp pipeline.

### Task 1: Add the extracted camp images

**Files:**
- Create: `source/img/camp/hands.png`
- Create: `source/img/camp/pool.png`
- Create: `source/img/camp/team.png`
- Create: `source/img/camp/mountains.png`

**Steps:**

- Copy the exact Figma asset files from `/tmp/logika-figctx/assets/images/` into `source/img/camp/` using the semantic names above.
- Verify the four files are PNGs and retain their original dimensions:

```bash
identify source/img/camp/*.png
```

Expected dimensions: `hands` 960x1280, `pool` 1280x960, `team` 960x1280, `mountains` 1280x960.

### Task 2: Create the camp page from the existing hero

**Files:**
- Create: `source/camp.html`

**Interfaces:**
- Consumes: `partials/head.html`, `partials/header.html`, `partials/footer.html`, shared `banner-section`, shared `main-form`, shared sprite icons.
- Produces: a buildable camp page at `build/camp.html` with one `.banner-section.camp-hero` and the existing form contract.

**Markup requirements:**

- Use the existing page shell and script includes from `source/index.html`.
- Use `section class="banner-section camp-hero"`.
- Replace the generic home title/subtitle with the approved Figma camp text.
- Replace the boy-character slot with four `<img>` elements inside `.camp-hero__gallery`, using `/img/camp/*.png` paths relative to `build/camp.html`.
- Keep the existing Logika character asset on the form side.
- Use three inputs: name, telephone, and city; preserve the existing `main-form__input` classes and submit button classes.
- Add the six-item camp information bar with the existing banner-bar icon assets where possible and the existing sprite/icon assets for the remaining items.

### Task 3: Add isolated camp styling

**Files:**
- Modify: `source/scss/blocks/sections/banner-section.scss`

**Implementation:**

- Keep all existing declarations unchanged.
- Add `.camp-hero` after the existing `.banner-section` block.
- Set the camp background to the Figma light blue (`#DDF0FB`) and preserve the violet form (`var(--violet-100)`).
- Arrange the left content as text plus the four-photo circular collage.
- Size the gallery images with `object-fit: cover`, `border-radius: 50%`, and a responsive grid/overlap that remains decorative on mobile.
- Keep the Figma white information bar as a shadowed horizontal row on desktop and allow horizontal wrapping/stacking at the existing mobile breakpoint.
- Do not modify selectors used by `source/index.html` or `source/about.html`.

### Task 4: Build and validate

**Files:**
- Generated: `build/camp.html`
- Generated: `build/css/blocks/sections/banner-section.css`
- Generated: `build/img/camp/*.png`

**Steps:**

- Run `npm run build` from `/home/sbaikov/Desktop/Projects/logika`.
- Confirm `build/camp.html` exists and contains the four camp image paths.
- Run `npm run html` and record whether the existing validator reports any errors.
- Check `git diff --check`.
- Start the existing static preview with `npm start` if needed and inspect `build/camp.html` at desktop and mobile widths.
- Confirm no source changes occurred in `source/index.html`, `source/about.html`, or shared form JavaScript.

### Task 5: Final review

- Review the diff for unrelated generated or Graphify files.
- Keep only the approved camp page, four camp assets, scoped SCSS, generated build output, and the approved plan/spec documents.
- Report the exact verification commands and any pre-existing validator warnings separately from camp-specific results.
