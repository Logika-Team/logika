# WordPress 404 Page Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task with verification checkpoints.

**Goal:** Replace the accidental Media Center fallback with a branded Ukrainian 404 page in the `wordpress` worktree.

**Architecture:** Use WordPress template hierarchy via a new theme-level `404.php`. Keep the existing global header/footer and load one scoped stylesheet only on `is_404()`; no JS, ACF, router changes, or dependencies.

**Tech Stack:** Classic WordPress theme, PHP 8.3, CSS, shell/PHP contract checks, DDEV.

## Global Constraints

- Work only in `/home/sbaikov/Desktop/Projects/logika/.worktrees/wordpress`.
- Preserve unrelated dirty files.
- Keep all user-facing copy Ukrainian.
- Use `home_url()` for links and escape dynamic output.
- Keep HTTP status `404 Not Found`, accessible focus states, and mobile layout.

### Task 1: Add the failing 404 contract

**Files:**
- Create: `tests/404-page.php`

- [ ] **Step 1: Write the contract test**

  Assert that `404.php` exists, calls the shared header/footer, contains one `h1`,
  Ukrainian 404 copy, home/course links, and that `functions.php` gates `404.css`
  behind `is_404()`.

- [ ] **Step 2: Run the test and verify RED**

  Run `php tests/404-page.php`; expect failure because `404.php` and `404.css` do not exist yet.

### Task 2: Implement the template and scoped styles

**Files:**
- Create: `wordpress/wp-content/themes/logika-theme/404.php`
- Create: `wordpress/wp-content/themes/logika-theme/assets/css/404.css`

- [ ] **Step 1: Add the minimal template**

  Render `get_header()`, a single `<main>`/`<h1>` content section, escaped
  `home_url( '/' )` and `home_url( '/it-courses/' )`, then `get_footer()`.

- [ ] **Step 2: Add the branded responsive CSS**

  Use existing `--violet-500`, `--violet-100`, `--yellow`, `--white`, `--font-primary`
  variables; create a solid-violet layout with yellow accents, a readable white panel,
  responsive spacing, hover/focus states, and `prefers-reduced-motion` support.

### Task 3: Load CSS only on 404 and verify

**Files:**
- Modify: `wordpress/wp-content/themes/logika-theme/functions.php`
- Modify: `docs/plan.md`

- [ ] **Step 1: Enqueue the stylesheet conditionally**

  Add one `filemtime()` versioned `wp_enqueue_style()` call inside `logika_theme_assets()`
  guarded by `is_404()`.

- [ ] **Step 2: Run the contract and syntax checks**

  Run `php tests/404-page.php`, `php -l wordpress/wp-content/themes/logika-theme/404.php`,
  `php -l wordpress/wp-content/themes/logika-theme/functions.php`, and `git diff --check`.

- [ ] **Step 3: Smoke-test the real DDEV route**

  Request `http://logika.ddev.site/this-route-does-not-exist-404/`; verify status `404`,
  `body.error404`, the 404 heading, both links, and the `logika-404` stylesheet.

- [ ] **Step 4: Update the project plan entry**

  Append a concise unchecked item describing the new 404 page without renumbering existing entries.
